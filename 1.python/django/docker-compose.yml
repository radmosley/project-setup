version: '3'

services:
  postgres:
    image: postgres

  django:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./api:/code
    ports:
      - "8000:8000"
    depends_on:
      - db