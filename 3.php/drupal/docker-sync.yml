version: "2"

syncs:
  docker-sync:
    src: './'
    sync_userid: '501'
    sync_excludes: ['.gitignore', '.git/', '.idea/']

  drupal_sync:
    src: './code'
    sync_strategy: 'native_osx'
    sync_excludes: ['.gitignore', '.git/', '.idea/']
  
  postgres_sync:
    src: './artifacts'
    sync_strategy: 'native_osx'
    sync_excludes: ['.gitignore', '.git/', '.idea/']